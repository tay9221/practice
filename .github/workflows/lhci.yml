on:
  schedule:
    - cron: "5 * * * *"
  workflow_dispatch:

jobs:
  test-cron:
    runs-on: ubuntu-latest
    steps:
      - name: Print current time
        run: |
          echo "Current Time: $(date)"
